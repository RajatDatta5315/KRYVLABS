5.  **Set Server-side API Key**: In your Supabase dashboard, go to **Project Settings > Edge Functions**. Add a new secret named `SUPABASE_OPENAI_API_KEY` and set its value to your OpenAI API key.

### 3. Database Setup

The database schema is defined in `supabase/schema.sql`.

1.  Go to the **SQL Editor** in your Supabase dashboard.
2.  Copy the entire content of `supabase/schema.sql` and run it. This will create all the necessary tables, policies, and functions.

### 4. Run the Application

Now you can start the development server:

